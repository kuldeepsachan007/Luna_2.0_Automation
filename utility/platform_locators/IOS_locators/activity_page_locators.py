from appium.webdriver.common.appiumby import AppiumBy

# TODO: Update with actual iOS locators when available

ACTIVITY_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Activity')
ACTIVITY_SCORE_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Activity Score')
BACK_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Back Button')
GOAL_PROGRESS_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Goal Progress')
TOTAL_CALORIES_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Total Calories')
STEPS_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Steps')
DISTANCE_BUTTON = (AppiumBy.ACCESSIBILITY_ID, 'Distance')
